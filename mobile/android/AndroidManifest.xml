<?xml version="1.0" encoding="utf-8"?>
<!--
  Merge into app/src/main/AndroidManifest.xml
  Replace placeholders from src/config/mobile-app.ts and src/lib/admob.ts
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <!-- AdMob uses GAID — declare in Play Console Data safety (see docs/mobile-store-compliance.md) -->
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

  <application
      android:allowBackup="true"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/Theme.Shirwell">

    <!-- AdMob app ID (ADMOB_APP_ID) -->
    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="ca-app-pub-2495432679632375~1624956947" />

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- OAuth custom scheme: shirwell://auth/callback -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="shirwell"
            android:host="auth"
            android:pathPrefix="/callback" />
      </intent-filter>

      <!-- App Links — requires live assetlinks.json on your domain -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="shirwell-bancan.vercel.app"
            android:pathPrefix="/auth/callback" />
      </intent-filter>
    </activity>
  </application>
</manifest>
