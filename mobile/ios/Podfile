# Shirwell Music — iOS dependencies
# SDK versions from src/lib/admob.ts → GOOGLE_MOBILE_ADS_SDK

platform :ios, '15.0'

target 'Shirwell' do
  use_frameworks!

  pod 'Google-Mobile-Ads-SDK', '~> 13.7.0'
  pod 'GoogleUserMessagingPlatform'
end
